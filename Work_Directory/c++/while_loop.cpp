#include <iostream>
using namespace std; 
int main ()
//{
	//int run = 10; 
	//while (run >= 0){
		//cout << run << endl;
		//run-=1;  
		//}
	//return 0; 
//}
{
	int run = -5; 
	do { 
		cout << run << endl;
		//run = run -1 ; 
	} while (run >= 0); 
	return 0; 
} 
// The difference between while and do while:
// do while runs at least once even if the condition is not satisfied 
// while loop checks the condition first and then after it runs 
