#include <iostream> 

using namespace std; 

int main () {
	string name = "Khalid"; //string is not a data type it is a class 
	// it methods such as find the size (length) of the string 
	cout << name.find("h");
	return 0; 
}
